# Hactto Client Development Guide

## Build & Lint
- Run linting:
  ```bash
  npm run lint
  ```
- Build production package:
  ```bash
  npm run build
  ```

## Git Guidelines
- All Git operations (commit messages, branch naming, PR titles, and descriptions) must be written in English.
